---
topic: To-Do-List — AI-assisted task manager (IBE160 solo project)
updated: 2026-09-10T21:57
---

- (decision) Product = To-Do-List, AI-assisted CRUD task manager; solo project for IBE160 course, submission end Nov 2026; grading weighted on AI feature implementation.
- (decision) AI role: agent suggests tags, priority level, short summary; user stays in control and accepts/edits/rejects each suggestion.
- (decision) Architecture posture: local-first, no login, tasks in local device storage; optional account + basic sync in v1; real-time multi-device conflict resolution out of v1. Targets web, desktop, mobile, tablet.
- (decision) Target users: students/classmates and individuals juggling many tasks across different parts of life.
- (decision) Q1 resolved: AI is an assistant that suggests (tags, priority, summary, smart lists); user accepts/edits/rejects. Earlier 'AI does almost everything' reinterpreted as AI removes manual metadata work, not autonomous action.
- (decision) Assignment scope: difficulty 'Simple'; login optional (required for sync/sharing); no e-commerce. Inputs: task text, due dates, project association, notes. Outputs: AI-suggested tags, priority, summary, smart lists.
- (decision) Open decision points carried from assignment, left open in brief: classification model vs rule-based; privacy choices (local vs cloud AI); archiving rules.
- (assumption) Q4 unanswered: assuming one responsive PWA spanning web/desktop/mobile/tablet, not separate native builds. Tagged [ASSUMPTION] in brief.
- (event) Fast path selected; drafting full brief with assumption tags.
- (decision) AI approach direction: rule-based/keyword suggestions as the cheap, offline, user-friendly baseline; optional upgrade to a cloud AI API (OpenAI/Claude) or a small local model. App itself runs locally. Exact provider still open.
- (decision) Privacy decided: no task data leaves the device unless the user opts into sync/sharing, which requires login. Cloud AI (if used) is opt-in on the same basis.
- (decision) Archiving decided: completed tasks archived after 30 days by default, user-configurable.
- (decision) Confirmed: single responsive PWA with local browser storage; notifications/reminders out of v1; dropped the 'accept > reject' success metric (needs usage logging, not worth it for scope).
- (event) Finalized: brief status set to done. Memlog audit: all 13 substantive entries are reflected in brief.md (no addendum needed). Prose/structure polish applied inline (trimmed duplicated suggestion-engine text).
- (event) Committed to git: root repo at project root (branch main, commit d88e22d) and copied into G16-shyam group repo (branch main, commit 73bb170, not pushed).
- (event by coach) Validation run against brief's own purpose; findings reviewed with user, approved for an Update.
- (decision by user) Confirmed solo project (user is the sole maker); the G16-shyam group repo is course logistics, not a scope change.
- (change) Removed the 'most AI suggestions accepted/lightly edited' user success signal — it had reappeared after being dropped earlier for needing usage logging; brief now consistent with that decision.
- (change) Fixed 'zero backend' overclaim: reframed as 'local-first by default' and stated plainly that opt-in sync/sharing/cloud-AI add a minimal backend. Optional account + basic sync stays IN v1 per prior decision — no scope reversal.
- (change) Open Decision Point + Solution now state the suggestion-engine choice carries grading weight (course leans on the AI feature) and a rules-only v1 may not meet it; flagged as the first thing to resolve in the PRD.
- (change) Tightened Problem section: competitors do ship AI tagging (usually paid); the free path still leaves sorting to the user. Grounded the 'people skip organizing' premise in the maker's own experience.
- (change) Clarified sync conflict handling: last-write-wins on conflicting edits with no merge UI in v1; only merge/conflict-resolution UI beyond that (and real-time multi-device sync) is out of v1.
- (event) Update applied to brief.md; status stays done, updated date bumped to 2026-09-10.
- (change) Executive Summary 'no backend' line softened to match the same fix elsewhere: core experience needs no backend; opt-in sync/sharing have a minimal one.
